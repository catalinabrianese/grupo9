const User = require()

function recordameMiddleware(req, res, next) {
    
    //req.cookies.recordame=req.body.id;

    //if (req.cookies.recordame != undefined &&
    // req.session.usuarioLogueado   )
}

module.exports = recordameMiddleware;